#include "pch.h"

int main()
{
	// init listener socket

	// bind
	// listen
	
	// client connected
	// main loop
		// print UUID hash & IP 
		// check if blacklisted (파일에서)
			// if yes -> Delete itself & request to Trigger BSOD 
		
		// loop
			// heartbeat
			// 5초마다 클라이언트 상태를 받아옴
				// if violation detected -> Delete itself & request to Trigger BSOD 
				// blacklist UUID hash (Mobo ID & Disk serial)



	// end socket
}